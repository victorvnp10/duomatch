## 1. Postgres schema (Supabase migrations)

- [x] 1.1 Create migration adding `couples` (id, two member user ids) and `activities`, `activity_selections`, `rewards`, `wishlist_items` tables, each foreign-keyed to `couples.id`, matching the fields read by `buildAchievementStats` (`packages/domain/src/services/achievement-stats-builder.ts`) and the `Activity`/`Reward`/`WishlistItem`/`CoupleData` shapes in `packages/domain/src/types.ts`. `supabase/migrations/0001_couples_and_activity_tables.sql`.
- [x] 1.2 Create migration adding `rounds` (id, couple_id, start_date, end_date, min_activities/min_challenges as individual `days`/`quantity`/`penalty` columns, `activities_last_checked`/`challenges_last_checked`) matching the `Round`/`RoundRule`/`RulesLastChecked` types, plus `couple_member_scores` for the per-partner score the round rules adjust. `supabase/migrations/0002_rounds_table.sql`.
- [x] 1.3 Create migration adding `couple_achievements` (couple_id, achievement_id, unlocked_at) with a unique constraint on `(couple_id, achievement_id)`. `supabase/migrations/0003_couple_achievements_table.sql`.
- [x] 1.4 Add RLS policies on every new table restricting row access to the couple's two member user ids (design.md - Couple data isolation), with `auth.uid()` wrapped in `(select ...)` per Supabase's RLS perf guidance. `supabase/migrations/0004_row_level_security.sql`.
- [x] 1.5 Add the `apply_round_rule_adjustment` `SECURITY DEFINER` Postgres function (single transaction: apply score deltas to both partners + update `rules_last_checked`) and a migration granting it only to authenticated couple members. `supabase/migrations/0005_apply_round_rule_adjustment_rpc.sql`. Note: Supabase grants `EXECUTE` to `anon`/`authenticated` by default on function creation regardless of `REVOKE ... FROM PUBLIC` — required an explicit `REVOKE ... FROM anon`, included in this file.
- [x] 1.6 Applied all 5 migrations to the connected Supabase project (`pwoivqbcxhdpfmeqcdfg`) via `mcp__supabase__apply_migration`. Verified with `list_tables` (all 8 tables present, `rls_enabled: true`) and `get_advisors`: security advisors clean except the expected/intentional "authenticated can execute `apply_round_rule_adjustment`" WARN; performance advisors clean except "unused index" INFO notices (tables are empty, not yet queried — not actionable).

## 2. Application layer ports and use cases

- [x] 2.1 Add dependency on `@duomatch/domain` (already present) — no new runtime dependency needed for ports themselves.
- [x] 2.2 Define `AchievementsRepository` and `RoundRulesRepository` port interfaces in `packages/application/src` (load inputs for `buildAchievementStats`/`evaluateNewAchievements`/`findActiveRound`/`evaluateCyclicalRules`; persist newly unlocked achievements; persist round-rule score adjustments). `src/ports/achievements-repository.ts`, `src/ports/round-rules-repository.ts`.
- [x] 2.3 Implement `evaluateAndPersistAchievements(deps: AchievementsRepository, coupleId: string): Promise<string[]>` use case: load data, call `buildAchievementStats` + `evaluateNewAchievements`, persist only ids not already unlocked, return the newly unlocked ids. `src/use-cases/evaluate-and-persist-achievements.ts`.
- [x] 2.4 Implement `evaluateAndPersistRoundRules(deps: RoundRulesRepository, coupleId: string, todayStr: string): Promise<CyclicalRulesResult | null>` use case: load active round + activities, call `findActiveRound` + `evaluateCyclicalRules`, persist the result via the repository (backed by the RPC from 1.5), return the result or `null` when no active round. `src/use-cases/evaluate-and-persist-round-rules.ts`.
- [x] 2.5 Removed the placeholder `assertPositiveIntegerInput` demo function from `packages/application/src/index.ts` (the barrel now exports the real ports/use-cases instead). Left `packages/domain/src/index.ts`'s `isPositiveInteger` export untouched, per the scope note added to this task.
- [x] 2.6 Wrote unit tests for both use cases using in-memory fake repository implementations (`evaluate-and-persist-achievements.test.ts`, `evaluate-and-persist-round-rules.test.ts`): no new achievements, one new achievement, already-unlocked not re-persisted, no active round, rule not due, rule due with adjustment persisted, re-running with unchanged data produces no writes. Also added `vitest`/`vite` to `packages/application` (it had no test setup before this change) — 7/7 tests pass.

## 3. Supabase infrastructure adapters

- [x] 3.1 Added `@supabase/supabase-js` dependency to `packages/infrastructure-supabase/package.json`. As anticipated by ADR-0003 ("adding a new legitimate third-party dependency in application or infrastructure-supabase will require editing dependency-cruiser.config.mjs"), also added an `allowed` rule for `packages/infrastructure-supabase → node_modules/@supabase/supabase-js` — `pnpm dep-check` failed with 4 `not-in-allowed` violations before this rule, 0 after.
- [x] 3.2 Implemented `SupabaseAchievementsRepository` and `SupabaseRoundRulesRepository` in `packages/infrastructure-supabase/src`, structurally satisfying the `packages/application` port interfaces via `packages/domain` types only (no import from `packages/application`, per architecture-governance — verified by `pnpm dep-check` in group 5).
- [x] 3.3 Mapped the Firestore-shaped domain types to/from the relational rows from section 1 in `src/mappers/activity-row.ts` and `src/mappers/round-row.ts` (`Activity.selections: Record<string, ActivitySelection>` built from the nested `activity_selections` PostgREST relation; `Round.rules.minActivities`/`minChallenges` built from individual `min_*_days`/`quantity`/`penalty` columns, present only when all three are non-null).
- [x] 3.4 Wired the round-rule score adjustment through `supabase.rpc("apply_round_rule_adjustment", ...)` in `SupabaseRoundRulesRepository.persistRoundRuleAdjustment` (design.md - Round-rule score application decision).
- [x] 3.5 Wrote adapter tests against mocked PostgREST responses (`src/test-support/fake-supabase-client.ts`, a minimal fake of supabase-js's chainable/thenable query builder — no local Supabase instance was available in this environment). 7 tests covering: mapped read shape, partnerId derivation, achievement upsert with `ON CONFLICT DO NOTHING` semantics (`ignoreDuplicates: true`), no write when nothing newly unlocked, round-rule read mapping, RPC call shape for a due adjustment, and RPC error propagation (partial-failure protection). The couple-isolation RLS scenario itself is exercised at the database layer, not through the mocked client — covered by task 1.6's `list_tables`/`get_advisors` verification that RLS is enabled with the policies from `0004_row_level_security.sql`.

## 4. Governance

- [x] 4.1 Added `docs/adr/0005-supabase-schema-persistence-achievements-round-rules.md` recording the Supabase schema/adapter conventions from design.md (normalized tables over JSONB, append-only achievements table with a uniqueness constraint, `SECURITY DEFINER` RPC for atomic score adjustment, the `(select auth.uid())` RLS optimization, the anon-EXECUTE default-grant gotcha, and the dependency-cruiser rule this change required).

## 5. Verification

- [x] 5.1 Ran `pnpm --filter @duomatch/application build/typecheck/lint/test/dead-code` and the same for `@duomatch/infrastructure-supabase` — build/typecheck/lint all pass with zero errors; `application` 7/7 tests pass, `infrastructure-supabase` 7/7 tests pass. `dead-code` (ts-prune) flags every new export as unused in both packages — expected and non-blocking, same as the precedent in `migrate-domain-achievements-round-rules`: nothing outside these packages consumes them yet, per this change's explicit non-goal (no `apps/mobile` wiring).
- [x] 5.2 Ran the repo-root `pnpm dep-check` — 0 violations (26 modules, 43 dependencies cruised), confirming `infrastructure-supabase` never imports `application` and the new `@supabase/supabase-js` edge is explicitly allowed (see 3.1/ADR-0005). Also ran repo-root `pnpm build`, `pnpm typecheck`, `pnpm lint`, `pnpm test` (all 5 packages) — all green; `apps/mobile`'s build initially failed because its placeholder imports `assertPositiveIntegerInput` from `@duomatch/application`, which task 2.5 had removed — fixed by keeping that placeholder in `packages/application/src/index.ts` (documented there) instead of touching `apps/mobile`, preserving this change's declared scope. 49/49 tests pass repo-wide.
- [x] 5.3 Ran `get_advisors` (security + performance) on the Supabase project after migrations — security: only the expected/intentional `authenticated` WARN on `apply_round_rule_adjustment`; performance: only `unused_index` INFO notices (tables are empty, not yet queried). No new or unexpected advisories.
- [x] 5.4 Confirmed via `git status`: diff is `supabase/migrations/**` (new), `packages/application/src/**` (ports, use-cases, barrel), `packages/infrastructure-supabase/src/**` (adapters, mappers, tests), `docs/adr/0005-*.md` (new), `dependency-cruiser.config.mjs` (the anticipated third-party-dependency rule), `pnpm-lock.yaml`, package/tsconfig edits for `application`/`infrastructure-supabase`, and this change's `openspec/` artifacts. No changes to `packages/domain`. `apps/mobile` has no diff either — only its existing import of `assertPositiveIntegerInput` kept working. Pre-existing untracked `.claude/worktrees/` and `.mcp.json` predate this change and are unrelated to it.
